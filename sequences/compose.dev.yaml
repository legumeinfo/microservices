services:
  sequences:
    build:
      context: .
    environment:
      GENES_ADDR: "${GENES_ADDR:-genes:8081}"
      DSCENSOR_URL: "${DSCENSOR_URL:-http://dscensor:8080}"
      DS_UTILITIES_URL: "${DS_UTILITIES_URL:-http://ds_utilities:8080}"
    ports:
      - "${PORT:-8082}:8080"
