services:
  ds_utilities:
    image: ghcr.io/legumeinfo/microservices-ds_utilities:1.0.0
    environment:
      ALLOWED_URLS: "https://data.legumeinfo.org/,https://data.soybase.org/"
    ports:
      - "${PORT:-8080}:8080"
    restart: always
