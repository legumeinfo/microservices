services:
  fasta_api:
    build:
      context: .
    environment:
      ALLOWED_URLS: "https://data.legumeinfo.org/,https://data.soybase.org/,https://gist.github.com/"
    ports:
      - "${PORT:-8080}:8080"
